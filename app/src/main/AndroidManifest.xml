<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="group.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogIn_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Welcome_Screen" />
        <activity android:name=".Register" />
        <activity
            android:name=".Services"
            android:label="@string/title_activity_services"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".serviceprofile"
            android:label="@string/title_activity_serviceprofile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Calendar_Activity" />
        <activity android:name=".Edit_Profile"></activity>
    </application>

</manifest>